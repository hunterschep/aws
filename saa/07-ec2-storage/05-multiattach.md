# Multi-Attach 

- Attach the same EBS to multiple EC2 instances in the same AZ (IO1/IO2)
- Use case: Higher application availability in clustered apps 
- Apps who must manage concurrent writes 
- Must use a cluster aware file system 
