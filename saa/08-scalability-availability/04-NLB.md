# Network Load Balancer 

Layer 4 load balancer: TCP and UDP traffic 
- Handles millions of requests per seconds + low latency 
- ONE static IP per AZ
- Can assign elastic IP for whitelisting per AZ 
- Health checks support TCP/HTTP/HTTPS 

